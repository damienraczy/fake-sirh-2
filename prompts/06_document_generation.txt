You are an expert HR data generator.

For the employee profile below, generate a set of realistic HR documents.

**Employee Profile:**
- Employee name: {employee_name}
- Position: {position}
- Sector: {sector}
- Culture: {culture}

**Instructions:**
1.  Generate between documents from the following types:
   - 1 CV, with employee name,
   - {nb_diploma} DIPLOMA with employee name,
   - {nb_certification} CERTIFICATION with employee name,
   - {nb_training_certificate} TRAINING_CERTIFICATE with employee name.
2.  The content of each document must be detailed, professional, and consistent with the employee's profile and the company's sector.
3.  The final output **MUST** be a single, valid JSON object containing a key "documents", which holds a JSON array of document objects.
4.  Each object in the array must have two keys: "document_type" and "content".

**Language:**
- output in {language}

**MANDATORY OUTPUT FORMAT:**
```json
{{
  "documents": [
    {{
      "document_type": "CV",
      "content": "..."
    }},
    ...,
    {{
      "document_type": "DIPLOMA",
      "content": "..."
    }},
    ...,
  ]
}}
```

Output only JSON.